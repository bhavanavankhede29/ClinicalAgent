# Optional "langchain" engine for POST /api/query/langchain.
# Pure-Python; installs on Python 3.14. (langchain-community does NOT — it pulls
# numpy, which has no 3.14 wheel and no compiler here.)
#
#   .\.venv\Scripts\python.exe -m pip install -r requirements-langchain.txt
langchain-core>=1.0
langchain-anthropic>=0.3
langgraph>=1.0
