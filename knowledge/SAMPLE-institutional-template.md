# SAMPLE — institutional protocol template

> ⚠ This is an empty template with placeholders, not medical guidance. Replace
> every `[bracketed]` value with your institution's approved content, rename the
> file, and re-run the index. Delete this file once you have real documents.

## Purpose and scope

This protocol applies to `[patient population]` cared for in `[setting / units]`
at `[institution]`. It is a local adaptation of `[national/society guideline
name, year]` and is reviewed `[frequency]` by `[committee]`.

Where this document conflicts with a general reference (FDA label, PubMed,
UpToDate, etc.), the treating team should follow this document for patients in
scope and record the rationale.

## First-line management

- Preferred agent / approach: `[drug, dose, route, frequency, duration]`
- Alternative if `[contraindication / allergy]`: `[alternative]`
- Do not use `[restricted agent]` without `[approval / stewardship consult]`

## Monitoring

- Baseline: `[labs / vitals / imaging]`
- During treatment: `[parameter]` every `[interval]`; hold or adjust if
  `[threshold]`

## Escalation and consultation

- Contact `[service]` if `[trigger]`.
- Transfer to `[higher level of care]` when `[criteria]`.

## Documentation

Record indication, the specific criteria met, monitoring plan, and the
responsible clinician. Human review is required before any patient-impacting
action taken on the basis of automated decision support.
